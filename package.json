{
  "name": "edudrive",
  "version": "0.2.0.2",
  "private": true,
  "description": "EduDrive - Cloud storage platform for students",
  "scripts": {
    "start": "docker compose up -d && npx concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start:app": "docker compose up -d && npx concurrently --names \"BACKEND,FRONTEND,APP\" --prefix-colors \"blue,green,yellow\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"node scripts/open-app.js\"",
    "start:desktop": "docker compose up -d && npx concurrently --names \"BACKEND,TAURI\" --prefix-colors \"blue,green\" \"npm run dev --prefix backend\" \"npm run tauri dev --prefix frontend\"",
    "build:desktop": "npm run tauri build --prefix frontend",
    "dev": "npx concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "db:test": "npm run db:test --prefix backend",
    "db:push": "npm run db:push --prefix backend",
    "db:studio": "npm run db:studio --prefix backend",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}